<article class="post">
    <div class="title">
        <h1>
            {{post.title}}
        </h1>
    </div>
        reviewed by {{post.user.username}} on {{format_date post.created_at}}
        [{{post.vote_count}} 🍑's]
        <br>
        [{{post.star_rating}} ⭐]
    <div class="row">
        <div class="col p-4">
            <img src="{{post.post_url}}" alt="">
        </div>
        <div class="col-8 p-4">
            {{post.post_content}}
        </div>
    </div>
    
</article>

<div>
    <h3>
        <p>{{comments.length}} 🗨️ Comment Section</p>
    </h3>
</div>

{{#if loggedIn}}
<form class="comment-form">
    <div>
        <textarea name="comment-body"></textarea>
    </div>

    <div>           
        <button type="submit" class="comment-btn">🗨️</button>
        <button type="button" class="upvote-btn">🍑</button>
    </div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
<script src="/javascript/upvote.js"></script>
{{/if}}